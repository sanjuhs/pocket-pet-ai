# Codex Working Notes

Follow `AGENTS.md` first.

## Best-Fit Work

- Integrate the web, Python, benchmark, and documentation surfaces.
- Keep builds, tests, lint, and CI reproducible.
- Diagnose failures from repository evidence before changing behavior.
- Make scoped edits and protect unrelated work in a shared tree.

## Before Handoff

Run the relevant commands in `agent.tech.md`, inspect `git diff`, update the changelog for user-facing changes, and state exactly what was and was not verified. Never infer deployment success from a local build.
